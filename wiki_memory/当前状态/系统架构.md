---
type: state
status: active
kind: architecture
importance: high
updated: 2026-08-31
topic: system-architecture
source_logs:
  - "[[日志/2026-08-31-初始化工程与远程仓库]]"
  - "[[日志/2026-08-31-修复文字掉落蝴蝶飞行与8秒花园]]"
  - "[[日志/2026-08-31-中心坍塌与花丛变形]]"
  - "[[日志/2026-08-31-局部坍塌传播与性能优化]]"
  - "[[日志/2026-08-31-蝴蝶分散飞行与参数重置]]"
  - "[[日志/2026-08-31-放大参数面板与蝴蝶分布实验台]]"
  - "[[日志/2026-08-31-围绕花朵飞行模式]]"
  - "[[日志/2026-08-31-只保留花朵轨道与环绕参数]]"
  - "[[日志/2026-08-31-指针扰动与花朵回归互动]]"
supersedes: null
---

# 系统架构

## 架构概览

页面采用 React 控制壳加原生 Canvas 场景引擎。React 管理播放状态、参数和面板显示；Canvas 在单个动画循环中绘制代码卡片、文字粒子、蝴蝶线稿和花朵线稿。

## 模块关系

| 模块 | 职责 | 依赖 | 入口 |
| --- | --- | --- | --- |
| `App.tsx` | 保存控制参数，连接引擎快照和面板 | React、SceneEngine | `src/main.tsx` |
| `AnimationCanvas.tsx` | 创建高 DPI Canvas、ResizeObserver、Pointer Events 和动画生命周期 | React、SceneEngine | `App.tsx` |
| `ControlPanel.tsx` | 播放、重播、时间轴、分组参数、蝴蝶轨道实验台、指针扰动实验台和恢复默认 | React | `App.tsx` |
| `scene.ts` | 场景状态机、局部传播与按列分组的坍塌调度、半隐式 Euler 更新和 Canvas 绘制 | Canvas API、类型定义 | `AnimationCanvas.tsx` |
| `types.ts` | Stage、PhysicsConfig、粒子和场景快照类型 | 无 | `scene.ts` |

## 数据流

用户操作先更新 React 参数。连续数值通过 ref 同步到 Canvas 引擎，避免每帧重新渲染 React 树。Canvas 指针事件将屏幕坐标转换为 720×960 设计坐标后直接调用 `SceneEngine.setPointer`，不经过 React state。引擎根据时间、指针和参数更新对象，并以低频快照更新面板上的阶段、进度和数量。

## 场景阶段

- `intro`：0.0 至 1.4 秒，显示代码卡片和植物轮廓。
- `falling`：1.4 至 3.35 秒，默认从代码卡片中心附近的一小处开始缺失，按空间距离向周围传播，再受重力形成下落字符流；控制台可以切换同列坍方、中心聚拢或波纹塌落。
- `morphing`：字符掉落到 650 至 776 的花丛高度后才淡出并生成蝴蝶；时间阶段通常落在 3.35 至 5.2 秒，但低重力参数下允许延后到安全兜底时间。
- `bloom`：5.2 至 8.0 秒，花茎、叶片和花瓣依次生长。

8 秒后时间轴保持在完成状态，蝴蝶使用不受时间轴上限影响的 `motionTime` 持续盘旋。

蝴蝶的飞行相位与扇翅相位分离，扇翅频率由 `PhysicsConfig.wingBeatFrequency` 控制，默认 2.8Hz，低于原先写死的快速频率。当前只保留围绕花朵飞行：每只蝴蝶使用自己的 `flowerX`、`flowerY` 作为轨道中心，`butterflyOrbitRadius` 和 `butterflyOrbitHeight` 分别控制水平和垂直半径，`butterflyOrbitSpeed` 控制轨道角速度，`butterflyOrbitTilt` 控制椭圆倾角，`butterflyOrbitWobble` 控制半径的周期呼吸，`butterflyOrbitDrift` 控制花头周围的低频漂移，`butterflyFlowerAttraction` 控制跟随花头的转向强度。飞行速度、大小和扇翅频率仍可独立调节。目标点固定绑定花头并保持在安全区域内，配置变化只影响已有蝴蝶的后续运动，不重建粒子、不重播；控制台“恢复默认”替换全部 React 参数，不强制重播。

花朵使用 `flowerWindStrength` 和固定随机相位产生低频自然风动；指针以花头为中心，在 `flowerPointerRadius` 内按 `pointerFalloff` 计算左右偏移，并由响应速度和回归速度控制无跳变的跟随。蝴蝶在 `butterflyPointerRadius` 内使用 `butterflyPointerRepulsion` 施加二维远离力，指针离开后由 `butterflyPointerReturn` 加速回到原花朵轨道。指针参数在完成态仍实时生效，指针状态不改变花朵绑定或时间轴。

`PhysicsConfig.collapseMode` 和 `collapseDuration` 控制文字变化形式与传播观察时长，默认是 `local-collapse` 和 1.4 秒；字符被波及后立即从源位置进入重力下坠，不追踪中心目标，也不设置中间汇聚阶段。

静态纸张背景在引擎内缓存，粒子扰动相位在生成时预计算，避免每一帧重复生成纹理和随机值。

## 运行与部署

开发环境使用 Vite，默认端口为 5173；若端口占用，Vite 会选择下一个可用端口。生产构建由 `tsc -b && vite build` 完成，产物位于 `dist/`，不包含参考视频。

## 相关决策

- [[决策/ADR-008-指针扰动与花朵回归互动|ADR-008-指针扰动与花朵回归互动]]
- [[决策/ADR-001-Canvas与React控制壳|Canvas 与 React 控制壳]]
- [[决策/ADR-002-状态替换与固定随机种子|状态替换与固定随机种子]]
- [[决策/ADR-005-局部坍塌传播与性能缓存|局部坍塌传播与性能缓存]]
- [[决策/ADR-004-中心坍塌与花丛变形触发|中心坍塌与花丛变形触发]]
- [[决策/ADR-006-完成态蝴蝶分散飞行与参数响应|完成态蝴蝶分散飞行与参数响应]]
- [[决策/ADR-007-蝴蝶分布实验台与大参数面板|蝴蝶分布实验台与大参数面板]]
